# Gateway (see web-backend/Gateway), no trailing slash
VITE_API_BASE_URL=http://localhost:4000

# Set to "true" to use the old mock DB + mock services (except chat stays mock-only in both modes).
VITE_USE_MOCK_BACKEND=false
